function [ uncoded_blocks ] = ChannelReceive( coded_blocks )
    dimensions = size(coded_blocks);
    uncoded_blocks = [];
    for i = 1 : dimensions(1);
        current_block = coded_blocks(i,:);
        current_block = RevertConvolutio
        
    end

end

